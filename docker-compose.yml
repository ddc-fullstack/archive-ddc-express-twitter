version: "3"

services:
  sql:
    build: ./sql
    volumes:
      - ddc-twitter-sql:/var/lib/mysql
    env_file:
      - ./docker-compose.env
    command: --default-authentication-plugin=mysql_native_password
    ports:
      - 3306:3306
volumes:
  ddc-twitter-sql: